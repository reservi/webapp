import styled from "styled-components";

export const Button = styled.button`
    all: unset;
    width: 7em;
    height: 2em;
    cursor: pointer;
    margin: 0 auto;
    line-height: 1.5;
    display: inline-block;
    margin-right: 10em;
    color: white;
`